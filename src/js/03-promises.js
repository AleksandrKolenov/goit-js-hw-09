// import Notiflix from 'notiflix';

// const prom = new Promise((resole, reject) => {
//   const cd = Math.random() > 0.5;

//   if (cd) {
//     resole('jnjiniuu (исполнен)');
//   }

//   reject('iuhhhu(отклонён)');
// })

// console.log(prom);


function even_or_odd(number) {
  for (let i = 0; i < number.length; i++) {
 if (number) {
   even % 2 === 0;
   return even;
 }if (number) {
   odd % 2 !== 0;
   return odd;
 }
  }
};

